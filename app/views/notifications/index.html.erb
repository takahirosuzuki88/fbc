<h2>お知らせ</h2>
<ul>
  <% @notifications.each do |notification| %>
  <hr>
    <li>
        <span><%= '未読' %></span>
        <p>
          <%= profile_img(notification.comment.user) %>
          <%= notification.comment.user.try(:name) %>さんが
          あなたのトピック（<%= link_to "#{notification.comment.blog.title}", blog_path(notification.comment.blog, notification_id: notification.id) %>）にコメントしました。
        </p>
        <p><%= notification.created_at %></p>
    </li>
  <% end %>
</ul>